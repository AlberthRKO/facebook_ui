import 'package:flutter/material.dart';

class ShoppScreen extends StatelessWidget {
  const ShoppScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Shopping Screen'),
      ),
    );
  }
}
